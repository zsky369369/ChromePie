buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:2.0.0'
    }
}

allprojects {
    repositories {
        jcenter()
    }
}

apply plugin: 'com.android.application'

android {
    signingConfigs {
        tgpark {
            keyAlias 'tgpark'
            keyPassword 't23gy6p2rk'
            storeFile file('V:/chromium_tgpark.keystore')
            storePassword 't23gy6p2rk'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.2"
    defaultConfig {
        applicationId 'com.jt5.xposed.chromepiese'
        minSdkVersion 16
        targetSdkVersion 23
        signingConfig signingConfigs.tgpark
    }
    lintOptions {
        abortOnError false
    }
    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
            java.srcDirs = ['src']
            res.srcDirs = ['res']
            assets.srcDirs = ['assets']
        }
    }
    productFlavors {
    }
    buildTypes {
        release {
            signingConfig signingConfigs.tgpark
        }
    }
}

dependencies {
    provided 'de.robv.android.xposed:api:81'
}
